---
name: project_moms_care_app
description: Mom's Care — DJ's walled-off PHI-grade eldercare app (own private repo + Render web + own Postgres + own auth, Odoo OUT). Built overnight 2026-09-21 by Builder-2, Lead orchestrating, Cheryl security-reviewing. Infra facts + conventions + gates.
metadata:
  node_type: memory
  type: project
  originSessionId: 93ae5c9a-b2db-49a9-8fa8-84d13000c2ae
  modified: 2026-09-22T06:47:59.735Z
---

**A separate PHI-grade family eldercare app (4 caregivers + Mom), fully WALLED OFF from W&SC** — own repo, own Render service, own Postgres, own session secret + Fernet key, **Odoo/Google/Drive/OAuth entirely OUT**. Same GitHub/Render account is fine (Cheryl's rule: the wall is around DATA+CREDENTIALS, not the account). Spec: `C:\Users\dj\MomsCare\MOMS_CARE_SPEC.md`; board `MOMS_CARE_BOARD.md` (M4/M5); Cheryl's review `MomsCare/DESIGN_REVIEW.md` (cheryl-workspace `MOMS-CARE-DESIGN-REVIEW.md` @5f152dff).

## Infra (provisioned 2026-09-21, DJ-authorized, cost pre-cleared)
- **Repo:** `windowandsolarcare-hash/momscare-app` (PRIVATE). Local build dir `C:\Users\dj\MomsCare\app\` (git repo; push via `git push` — NEW repo, no branch protection, unlike W&SC's gh-api-only rule). ⚠ the fleet OAuth token lacks `workflow` scope → CANNOT push `.github/workflows/` (gitleaks CI is staged at `security/gitleaks-workflow.yml` as a template DJ enables).
- **Render web:** `momscare` = `srv-daocjjbm8hqs73e5hu0g` → https://momscare-m0hp.onrender.com (Oregon, starter, autoDeploy from main). Runtime python, `uvicorn main:app`.
- **Render Postgres:** `momscare-db` = `dpg-daocj0btqb8s73eq8kt0-a` (Oregon, basic_256mb, PG18) — ★ SEPARATE from W&SC DB; **ipAllowList EMPTY = internal-only** (external SQL BLOCKED, SSL-required). So verify its data via the APP's own endpoints, NOT the render PG-query MCP (which can't reach it — that's the wall working).
- **Env (Render, secret-clean — never printed/committed; local copy `C:\Users\dj\mc_secrets_momscare.txt`):** MC_DATABASE_URL (own DB), MC_SESSION_SECRET (own, never wsc_session), MC_FIELD_KEY (Fernet, PHI field enc), MC_SETUP_TOKEN (gates the one-time seed). Set via Render API with shell-resolved values.

## Stack + conventions (FastAPI, flat repo root, absolute imports)
- `db.py` single-conn+lock + `ensure_schema()` (idempotent, all tables day-one); tables `mc_users/mc_grants/mc_sessions/mc_audit/mc_meds/mc_med_history/mc_symptom_logs/mc_adherence/mc_requests/mc_messages/mc_emergency/mc_consent/mc_documents/mc_login_throttle`. Boots DORMANT if MC_DATABASE_URL unset.
- `crypto.py` Fernet field-enc on `*_enc` PHI cols (fail-closed if MC_FIELD_KEY unset).
- `auth.py` — OWN session secret; PBKDF2 **600k**; server-side sessions (revoke=instant offboard); ★ least-privilege BY DATA CLASS deny-by-default: `allowed(user, data_class, write)` → no grant row = DENY (owner short-circuits full). Data classes: symptoms/adherence/meds/documents/coordination/admin. Login throttle/lockout (5 fails→exp backoff). `reauth()` step-up. `audit()`.
- `main.py` — `require_class(data_class, write)` dep (deny+audit); /api/login (pw or Mom PIN), /logout, /me; **/api/admin/export + /delete_all need STEP-UP re-auth** (password) + owner; /api/setup/seed (MC_SETUP_TOKEN, idempotent-fresh-only); **/api/mom/today** (role=mom-only server-assembled minimal view — Mom has NO meds grant). Screen routers `from routers import ...`.
- `seed.py` — 100% FICTIONAL (persona **Anita Saunders**; NEVER the real person's name/data — Lead: the name isn't a discrepancy, it's PHI-safety; real data waits for DJ's demo→real flip, encrypted, never committed). Mom grants MINIMAL (symptoms:w, adherence:w, coordination:rw). Owner=dj/demo-owner-pw; caregivers karen/mike/susan; mom=anita/PIN 1955.

## Status + gates (2026-09-21)
- ✅ Repo + security SPINE (Lead HARD-QC pass + all 6 Cheryl-rubric fixes: 600k PBKDF2, throttle/lockout, export/delete re-auth, doc-storage-design signed-URL/metadata-only, crypto threat-model, gitleaks template) + provisioned + isolation VERIFIED + fictional seed + spine proven end-to-end (Mom-confinement 403 by-URL, re-auth 401-without-pw, med-history append, Fernet round-trip, creds-excluded-from-export).
- ✅ ALL 8 screen routers COMPLETE + Lead full-backend QC PASS (meds edit-append, symptoms, adherence, emergency[view meds/edit admin], requests[atomic claim-lock 200/409], channel[sender=session], ask[deterministic, NO LLM], overview) — every route require_class-gated, identity from session not body, audit + Fernet PHI + parameterized. Mirrored to `saunders-render-app/review/momscare/routers/` for Cheryl cond-1.
- ✅ Cheryl hardening #3 (owner 2FA/TOTP) VERIFIED LIVE (deploy dep-daod246q1p3s73d5maqg): pyotp enroll→verify-to-enable→login-enforce(need_2fa 401 w/o code)→disable(reauth-gated); caregiver enroll=403 owner-only; `twofa_enabled` idempotent ALTER applied; no login regression. Demo owner left password-only (disabled post-test). #4 (Cache-Control no-store on /api PHI) + cond-2 (canonical 6 data-class vocab locked) also done.
- ✅ HTML port (flag 3) DONE + verified live (commit ec93d02): `static/index.html` data-free — 4 gates pass (0 hardcoded PHI arrays, 0 localStorage seed, Mom NO switcher/own-screen-only, no AI/LLM, all data via authed /api fetch; api() helper wraps as {status,ok,data:<full json>} so client `res.data.X` reads top-level fields). More-nav = FIXED popover over bottom nav (was position:absolute → only reachable at bottom-scroll; now inset:0 fixed + tap-outside backdrop). Fictional persona name genericized out of all UI copy. Demo artifact ref: claude.ai/artifact/B6ZG2WoAAuCHrkdm1j4tGD.
- ✅ CHECK-IN same-day double-entry (Lead-designed, append-only) DONE + verified: `mc_symptom_logs.entry_kind` ('checkin'|'update'|'correction') via idempotent ALTER — intentional same-day re-entry APPENDS a labeled row, NEVER overwrites (mirrors med-history immutability; local_uid still dedups accidental retries). `/api/symptoms/today` → latest + count + full same-day progression (caregiver clinical value: 9:14 Fine → 3:02 Bad). `/api/mom/today` → checkin.at + checkin_count (Mom "already checked in today ✅ + time" indicator + ONE-TAP "Something changed → update"). Verified: count 2→3, kinds [checkin,checkin,update]. ⚠ symptoms are APPEND-ONLY = NO delete endpoint (a verify-update test row id=7 remains in the fictional seed by design). Lead re-mirrors symptoms.py for Cheryl's quick confirm (invariants preserved: require_class('symptoms')+append-only+audit+Fernet+local_uid). Cheryl design-note LOGGED in ask.py/overview.py (meds-gate surfaces symptom aggregates — refine per-class only if DJ wants meds-read-without-symptoms).
- ✅ AUDIT PHASE-1 WALK fixes (F1–F4, all live-verified): F1 /api/emergency MERGES partial updates (empty body→400, unsent fields kept — was blanking the paramedic sheet); F3 delete_all reauth() BEFORE confirm-token; F2 Mom check-in local_uid minted ONCE per entry + _momSaving guard (double-tap can only dedup) + chat/need guards; F4 central api() 12s AbortController timeout + Mom check-in durable localStorage intent + idempotent retry-on-load. F5 (approved): Mom GET /api/requests scoped to created_by=self (caregivers keep full queue).
- ✅ MANAGE-USERS (routers/admin.py, owner-only 'admin', real-flip PREREQ): GET /api/admin/users + POST /{id}/deactivate (active=FALSE + auth.revoke_all_for_user = INSTANT offboarding, spec §5a — was code-only) + /reactivate; guards (no self/owner deactivate); owner Admin UI list w/ Deactivate/Reactivate. Verified live (deactivate→"account disabled" login block→reactivate→restored).
- ✅ PHASE 2 CARE ASSISTANT BUILT (flag OFF, Lead gate-QC PASS both milestones + Cheryl-pending): openFDA drug-label + RxNorm (drug-NAME only, zero PHI, cached in mc_drug_facts_cache); 5 gates as CODE — redflags.py (deterministic PRE-model, escalation-biased, 41/41 tests incl. bloody/fall/unresponsive + blood-pressure-excluded), drugfacts.py (openFDA '+OR+' not '+', OTC files under 'warnings'), compose.py (isolated MC_ANTHROPIC_KEY, anthropic==0.122.0, temp0, no tools, fail-closed), postcheck.py (anti-fabrication: citation+safety markers required + drug-not-in-list/facts suppressed incl. LOWERCASE strong-ending generics, id-scrub), routers/assistant.py (require_class('assistant') deny-by-default, Mom 403, raw question never leaves server, audit=categories/counts only). MC_ASSISTANT_ENABLED default OFF. Grants: seed caregiver+=assistant:r + idempotent boot backfill (Mom excluded). Caregiver-only Assistant UI. Demo cheryl caregiver tester seeded (fictional) via /api/setup/add_caregiver.
- ✅ ASSISTANT CONTROLLED QC PASSED (2026-09-21, DJ set MC_ANTHROPIC_KEY + authorized the flip-on-test-flip-off): all 6 live — #1 composed answer carries Based on:/Source: openFDA/safety-frame; chest→911; ★ self-harm→988 (R-1 fix LIVE); overdose→Poison Control; gabapentin(not-in-list)+off-topic→suppressed. Cheryl CLEARED flag-on. Flag left OFF = now DJ's flip-on call. ★★ TWO COMPOSE GOTCHAS fixed (both hidden by compose's silent except→None; now it LOGS the failure): (1) `anthropic` was MISSING from requirements.txt → ModuleNotFoundError (add anthropic==0.122.0); (2) ★ Claude Sonnet 5 DEPRECATED the `temperature` param → API 400 "temperature is deprecated for this model" → REMOVED temperature from messages.create (determinism from system rule + fetched-facts-only + post-check, not a temp knob). Env flip is safe via Render MCP update_environment_variables (MERGES by default, replace=false — won't wipe other secrets).
- ✅ PHASE 3 buildable set DONE + verified live (commit 58d51bd, fictional sample data seeded): **1a MED DETAIL** (GET /api/meds/{id}/detail — DETERMINISTIC/NO-LLM reuse of drugfacts.py: side_effects=adverse_reactions + interaction warning=med's OWN drug_interactions+warnings+boxed_warning, NOT pairwise; +new mc_meds.frequency; no assistant flag needed — direct label display + safety frame + openFDA cite). **2 APPOINTMENTS** (new data_class 'appointments' caregivers+owner rw / Mom via /api/mom/today next_appointment; mc_appointments[subject Fernet]+mc_appointment_history append-only; routers/appointments.py next/upcoming/history/create/edit/soft-cancel). **3 HEALTH EVENTS** (mc_health_events append-only, notes Fernet, soft-remove, cats immunization/flu_shot/procedure/lab/other, data_class 'meds'; routers/health.py). UI: new "Health" nav tab + med "Details" modal + Mom's next-appt card. GATED (need doc-storage foundation): 1b pill IMAGE + 4 document scan-in (crown-jewels, Cheryl-reviewed encrypted-blob build).
- ✅ GUIDED CHECK-IN piece (i) COMPLETE + verified live (commits f09c558/b95325c/9dc9725), DETERMINISTIC no-LLM: **(i-a)** redflags.py surgically EXTENDED (Lead QC pass) — full BE-FAST (Balance+Eyes), self-harm→988 (CRISIS_CARD + crisis_scan, before the 911 scan), fall_assessment (head-hit→HEAD_WATCH 48h; +blood-thinner→red), sudden_change_redflag; 65/65 tests. **(i-b)** POST /api/checkin (routers/checkin.py) — SERVER-AUTHORITATIVE guardrail routes 911/988/head-watch cards + FAMILY ALERT to the channel (non-PHI reasons; ANY reported fall alerts even a no-injury 'log' fall); EXTENDS mc_symptom_logs (mood/pain/sleep/bowel_type/bowel_count/answers JSONB/freeform_enc/red_flag cols; bowel→diarrhea + pain→stomach mapped, categorical PLAIN [Lead+Cheryl-batch confirmed], freeform Fernet); require_class('symptoms',write)=Mom. **(i-c)** Mom guided UI: mood→acute(if not fine)→core(pain/eating/bowel+stepper/sleep)→free-form + always-available "🆘 I fell" triage; red-flag card w/ 911/988 tel: buttons; preserves Audit F2/F4. ✅ piece (ii) DONE+verified (d3eba1c/a2d0a04): mc_care_config + routers/care_config.py (12-module CATALOG = one source of truth + GET/POST); caregiver UI (Health tab "Daily check-in setup") toggles modules + blood-thinner; ★ checkin.py fall_assessment reads on_blood_thinner from CONFIG server-side (head-hit→red only when caregiver sets it — verified toggle on/off); Mom's flow renders only HER modules; SUDDEN-vs-GRADUAL confusion split verified (dementia worse+sudden→red, worse+gradual→logged). ✅ piece (iii) DONE+verified (227b9bb): mc_med_effectiveness(purpose_enc,unique/med)+mc_med_effect_logs(better/same/worse,notes_enc,append-only); routers/med_effect.py GET(purpose+trend+latest)/POST purpose/POST rating; UI "Is it working?" in med Details (caregiver observation, not clinical claim); tables added to export+delete_all. → GUIDED CHECK-IN FULLY COMPLETE (i+ii+iii, all Lead-QC'd + live-verified). Cheryl clinical review BATCHED at pre-live gate = the whole clinical surface: redflags.py + drugfacts.py + assistant/compose/postcheck + med-card detail + checkin.py/care_config.py/med_effect.py.
- ✅ CHERYL REVIEW (safety design BLESSED in substance) — 2 REQUIRED items RESOLVED (commit 4bc96e5): **R-1** (real hole she caught, my miss): assistant.py now runs crisis_scan+poison_scan FIRST in its pre-model gate (self-harm→988 was falling through to fallback). **R-2** (2 real data-exit gaps fixed): main.py was missing mc_care_config from export/delete_all + freeform_enc from _ENC_COLS (guided free-text would've exported as CIPHERTEXT). ★ SHOULD-a folded (live-verified): redflags += anaphylaxis/severe-vomiting/dehydration→911 + POISON_CARD/poison_scan (overdose/double-dose→Poison Control 1-800-222-1222), routed after crisis before the 911 scan in assistant+checkin; tests 80/80. Q6 CONFIRMED: momscare-db ipAllowList=[] (internal-only) + Render AES-256 at-rest + app Fernet. SHOULD-b (openFDA snippet next to paraphrase) = assistant flag-on follow-up. → Cheryl closes sign-off after Lead re-mirror. Specs: review/momscare/CHECKIN_SPEC.md + CHECKIN_CONTENT_RESEARCH.md.
- ✅ DJ demo tweaks (2026-09-21): "Today's pills" now COLLAPSIBLE on Mom's screen (tap-header ▾/▸, default expanded, persists localStorage). ★★ GOTCHA/FIX: the SPA shell (index.html served by StaticFiles) had NO cache-control → the PHONE served STALE JS after every deploy (masked the check-in tap fix for a whole cycle — DJ tested a cached pre-fix version). FIX: middleware sets `Cache-Control: no-cache, must-revalidate` on text/html (revalidate via ETag; /api PHI no-store unchanged). Lesson: always cache-control the SPA shell no-cache, else phone testers get stale builds. Also: iOS "taps don't stick / on-off" on big-button UIs → `button{touch-action:manipulation}` (kills double-tap-zoom tap-delay/eat) + `-webkit-tap-highlight-color:transparent`. Reproduce UI-interaction bugs by driving the LIVE app in claude-in-chrome (elementFromPoint + real computer.left_click).
- ✅ CONVERSATIONS/MEMORY PHASE 1 backend BUILT + live-verified (commit 365b983, Lead-QC pending), DETERMINISTIC no-LLM: **mc_conversations** (kind-tagged conversation/phone_call/in_person_visit CHECK; body_enc+transcript_enc Fernet; with_whom/topic/location/happened_at PLAIN for list/filter/search; append-only soft-remove active=FALSE; 2 partial indexes). New **data_class 'conversations'** (auth.DATA_CLASSES) — caregivers+owner rw, **Mom NOT granted** (she's the subject, caregiver-logged); seed caregiver grant + idempotent boot backfill (`_BACKFILLS`, role IN owner/caregiver). **routers/conversations.py**: POST create / GET list(kind filter, newest-first) / GET one / POST {id}/delete (soft) — all require_class('conversations'), created_by from session, audit + Fernet + parameterized. ★ Unified recall = **Ask fold-in** (routers/ask.py): when the structured record branches miss, falls through to `conversations.search_conversations(q)` (keyword overlap + require-match, gated on the caller ALSO having 'conversations' read = least-privilege; returns cite-style `matches`) BEFORE the fixed fallback — kept SEPARATE from the medical Assistant. main.py: mc_conversations added to export+delete_all tables + transcript_enc to _ENC_COLS (real-flip invariant). Live-verified: unauth 401; caregiver create×3/list/kind-filter/get-decrypt/soft-delete(→404)/Ask "glasses"+"grab bar" cite correctly; **Mom 403 on conversations GET+POST+ask** (least-privilege holds). 3 fictional demo entries seeded via API (one of each kind) since run_seed only fires on a fresh DB. ✅ **Lead backend QC = PASS** (append-only+Fernet+real-flip invariants+Ask dual-gate verified). ✅ **Piece B UI SHIPPED + live-verified end-to-end** (commit d61092c): new "Notes" nav tab (overflow, owner+caregiver; Mom's screen untouched) — Add-a-note modal (kind/who/topic/note/location[visit-only auto-reveal]/optional-when), All/Talks/Calls/Visits filter chips, tap-row read-only detail modal, soft-delete Remove, double-tap guard. Drove the LIVE app in chrome: nav has Notes, 3 rows render w/ icons+meta+snippet, Calls filter→1 row, real form Save creates a row (newest-first) + modal closes, soft-delete restores demo to 3. ★ **VOICE auto-transcribe = DEFERRED to a fast-follow (Lead decision, DJ flagged): TYPED-ONLY v1** — caregiver audio about Mom = PHI, so any 3rd-party STT needs a HIPAA BAA + Cheryl PHI-path review before routing audio off-box; transcript_enc col + detail-modal transcript render left as the clean seam. ✅ **CHERYL REVIEW CLOSED (2026-09-22, 3 items done):** (1) **topic+with_whom NOW ENCRYPTED** (commit 854b28f, live-verified): mc_conversations DDL → topic_enc/with_whom_enc (Fernet), location+happened_at stay plain; idempotent ADD-COLUMN migration + a one-time Python `_encrypt_conv_plaintext` in ensure_schema that Fernet-encrypts any legacy plaintext then DROPs the plaintext cols (idempotent via information_schema — no-op after first run; needs `import crypto` inside db.py); router encrypts on write + decrypts topic/who on list/get/search; _ENC_COLS += topic_enc/with_whom_enc. Verified: existing 3 demo rows re-encrypted + still display right, new writes encrypt, Ask searches encrypted fields, no boot errors. (2) mirrored conversations.py+db.py+auth.py+seed.py → review/momscare/ for her 5-check close. (3) confirmed `appointments` is in auth.DATA_CLASSES + seed caregiver _GRANTS + db _BACKFILLS. → **Real-PHI flip cleared from Cheryl's side for the whole surface incl. conversations.**
- ⏸ PRE-REAL-PHI-FLIP checklist (fictional now): (0b) voice auto-transcribe fast-follow needs HIPAA BAA + Cheryl PHI-path review before any STT vendor; (1) caregiver MUST set the blood-thinner flag + choose Mom's condition modules (/api/care_config) or the head-hit→same-day upgrade + condition Qs won't run; (2) disable/rotate /api/setup/seed + /api/setup/add_caregiver (+MC_SETUP_TOKEN); (3) DJ enables his own 2FA; (4) doc-storage delete_all-purge when files land; (5) DJ provisions MC_ANTHROPIC_KEY (assistant) + flips MC_ASSISTANT_ENABLED after Cheryl bless.
- ⏸ PRE-REAL-PHI-FLIP (hardening #5, NOT overnight): disable/rotate `/api/setup/seed` + `/api/setup/add_caregiver` (+ MC_SETUP_TOKEN) before any real data lands. P3: wire delete_all to purge encrypted doc BLOBs by storage_key when files land (+ add mc_appointments/mc_appointment_history/mc_health_events to the export/delete_all table lists).
- ⏸ WAITS FOR DJ (gated, do NOT do overnight): 4 real family logins; demo→real-data flip (gated on Cheryl's security BLESS); the Care Assistant (Phase 2, hard-gated — licensed interaction DB + zero PHI retention + non-overridable 911 guardrail; OFF now).
- Build gotcha: `psycopg[binary]==3.2.3` NOT on this Render Python's wheel index (only 3.2.10+) → use `psycopg[binary]==3.2.13`.

Related: [[feedback_never_relay_credential_via_session]], [[feedback_gh_push_empty_file_guard]], [[feedback_durable_foundation_over_shortcut]].
