---
name: project_hud_one_roof_spec
description: "MASTER STRATEGY (2026-07-26) — HUD \"one roof\" app spec. Read before any new-feature architecture talk. Spec at 3_Documentation/HUD_MASTER_SPEC.md + artifact."
metadata: 
  node_type: memory
  type: project
  originSessionId: 4ab12b63-cc8f-44de-b410-58b38aa2a6c9
  modified: 2026-07-27T07:23:04.223Z
---

DJ's north-star restated 2026-07-26: replace his ~10 weekly third-party apps under one roof; phone = heads-up display (data COMES to him). Personal + business ERP for entrepreneurs, incl. continuous learning. Full spec: `3_Documentation/HUD_MASTER_SPEC.md` (local) + artifact https://claude.ai/code/artifact/d6667c7a-88dd-4fbf-9dc9-88b6b18a27d2

Four foundation blocks: (A) priority engine + morning/evening briefing pushes — ONE ranked attention feed from all subsystems, build FIRST (existing data only); (B) real auth — prerequisite before passwords/bank data ever enter the app (see [[project_render_app_no_auth_layer]]); (C) Vault overhaul (see [[project_vault_overhaul_spec]]); (D) comms inbox — gated on Twilio port.

App verdicts: REPLACE Evernote, password-sheet (post-auth), news; FEED Angi/Thumbtack (parse lead emails → interrupt push w/ drafted reply, speed-to-lead = killer feature), banking, Gusto (keep for tax compliance, one-tap export); ASSIST social (GBP+Nextdoor first, draft posts from job photos), Spotify (learning layer + takeaway capture), Amazon.

Domains: wscare.pro already points at the Render app → HUD/owner pages live there, stop using onrender.com. windowandsolarcare.com = marketing. scenicartprint.com = Saunders.

Build order: 1 priority engine/briefings → 2 auth → 3 Vault V1-V4 → 4 lead feed + context lenses → 5 comms (Twilio-gated) → 6 money/credentials/Gusto (auth-gated) → 7 social/learning/resurfacing.

**Why:** every architecture conversation should ladder up to this; features that add a screen without removing a "go check" trip fail the test.
**How to apply:** before proposing/designing any new feature, check which block/phase it belongs to and whether it's gated (auth, Twilio). Discussion happened while another agent worked on goals — no code was written from this spec yet.

DJ APPROVED all recommendations 2026-07-26. Decisions locked: D1 notes in our own format (Drive keeps files/scans); D3 social = GBP+Nextdoor first; D4 interrupt pushes = leads, phone calls, texts, anything urgent (texts gated on Twilio port; calls ring natively, HUD adds missed-call+voicemail-transcript push); D6 merge Reference cards + pinned into one Vault pinned system. Still OPEN: D2 bank feed mechanism, D5 auth design session, briefing times.

MERGED 2026-07-26 with the goals agent's handoff `3_Documentation/OPERATING_SYSTEM_VISION.md` ([[project_operating_system_vision]]). Key merge points now IN the spec (§5b): approval inbox = HUD attention feed (ONE surface; approval item = attention item w/ draft); specialists = handbook+job-description+tools+model+trust-dial cards behind the feed; event-triggered never watching; weekly chief-of-staff digest joins daily briefings in Phase 1; AUTH now gates the approval inbox itself (OS doc's gap — money/customer actions can't launch from public endpoints); specialist lineup payroll→digest→accounting dodges the Twilio gate; cockpit last. Build sequence rev.2 = 8 phases (see spec §8). I am LEAD (smaller context by DJ's design).

WORKING AGREEMENT locked 2026-07-26 (spec §5c): lead session owns priority engine/HUD feed/briefings/approval-inbox surface/feed.py/auth/Vault/lead-event wiring/domains; goals-OS session owns specialist builds (payroll→digest→accounting), voice selector, PM evolution, in goals.py + v2_goals.html + NEW specialist modules. Seam = `3_Documentation/ATTENTION_FEED_CONTRACT.md` v1 (item shape; submit_item/delete_item in feed.py = stable API; specialists never push/render; customer-facing always kind=approval; one item per subject; withdraw stale items). Approval inbox must be a NEW file, not myday.py additions. Neither session edits the other's lane without a heads-up through DJ; breaking contract changes need both + DJ. Goals-OS session ACCEPTED the contract 2026-07-26 ("agreed, no questions that block") — handshake complete. feed.py shipped live same day; goals session's payroll specialist submitted its first real approval item through it.

BILLING FOLLOW-UP SPECIALIST assigned to goals-OS session 2026-07-26 — **PRIORITY over booking specialist**. Brief: `3_Documentation/BILLING_FOLLOWUP_BRIEF.md` (CORRECTED same day). ★ KEY PROCESS FACT from DJ: **jobs get marked Done only AFTER billing** — Done is the tail of the billing flow and fires Phase 5/6; so stuck jobs = PAST-DATE + NOT-Done (never "Done+unpaid"), and an unbilled job also silently blocks the customer's next maintenance cycle. Detection must disambiguate worked-awaiting-billing vs skipped-needs-reschedule (evidence: timer/photos/GPS; else 2-button card). Approve = send text ONLY (copy-paste into Workiz), NO invoice — invoice+payment+Done happen when money lands (Phase II: bank-watch → auto invoice/pay/Done). DJ's exact Venmo/Zelle template captured verbatim in the brief → ir.config_parameter wsc.billing.payment_request_tpl.

BOOKING SPECIALIST assigned to goals-OS session 2026-07-26 (DJ: booking text-reachouts is his current struggle; screenshot-of-thread intake until Twilio, permanent inbound responder after). Lead's handoff brief with all reuse pointers: `3_Documentation/BOOKING_SPECIALIST_BRIEF.md`. Output = interrupt approval card `booking:<id>` in the feed; execution via canonical book paths, SubStatus 'Scheduled' never 'Send Confirmation - Text'; DJ copy-pastes the reply until the port.

TWO-AGENT FILE LEDGER (fetch live before ANY edit; heads-up through DJ before touching the other's additions): `main.py` (both register routers); `dashboard.py` (goals-OS: stripe helper extraction L4936, email_from fix L5296, latest_charge; settle hooks coming); `cron.py` (goals-OS added reschedule daily refresh to _run_daily_sync; billing trigger coming); `v2_command.html` + `schedule_hub.html` (goals-OS: _isSkipped now excludes billing's awaiting jobs via /api/billing/awaiting_ids); `v2_timeclock.html` (payroll-review hook); `v2_apps.js` (lead owns — launcher/self-inject/resume-reload/FAB badge).

MAIL + CHANNELS (2026-07-26 night): AGENT_MAIL.md MOVED to app repo `saunders-render-app/3_Documentation/AGENT_MAIL.md` (specialists session couldn't see the local one; local file = pointer now; CLAUDE.md updated — read app-repo mail + app-repo SHARED_MEMORY.md at session start too). Specialists session's channels: app-repo SHARED_MEMORY.md (its notes), BILLING_SPECIALIST_STATUS.md (two-way thread), HUD_BADGES_SPEC.md. NOTE: app-repo SHARED_MEMORY.md is a PARALLEL file to the canonical Odoo-Migration one Render-Claude loads — watch for drift.

BADGE TRAIL + REUSABLE REGISTRY (DJ 2026-07-27: "should be a clear path telling where the 3 new live" + "will use badges again outside HUD"): full trail = red pill on 🚀 FAB → red pill on the app's ROW inside the open launcher → per-card pills on the destination page. Generic mechanism in v2_apps.js: `window.WSCRowBadges[href] = count` badges ANY launcher row (fabBadge sets the HUD's; live-updates an open list). Future features badge their row with one line — never build a bespoke badge again.

BADGES + UNSNOOZE built by lead 2026-07-26 (his spec): contract now has optional `badge` int + `/owner/api/<source>/mark_seen` convention (never auto-clear on open); GET /api/feed/badge sums for the 🚀 FAB pill on every page (v2_apps.js, 60s cache, busted by HUD actions) + navigator.setAppBadge PWA icon. HUD: red "N new" pill per card, 👁 Mark seen button, snooze Undo toast (6s), "⏰ Snoozed" section w/ Bring back (list?include_snoozed=1). Specialist #4 = reschedule (specialist_reschedule.py, card reschedule:review, only billing-marked skips — CC Skipped stays separate).

BILLING SPECIALIST STATUS 2026-07-26: built+deployed, NOT live (no cron, sweep not called). DJ changed model: ONE `billing:review` attention card → his /owner/api/billing/review decision page (Zelle/CC/Reschedule/Hold per job, auto-route = suggestion chip only). First real run: 8 past-due $1,195 (7 worked, Elsa Sandvold ambiguous; Jose Merelies→Stripe). Lead approved surface as canonical (no HUD rebuild), gave settle hook points + green light, approved cron placement. Full thread: saunders-render-app `3_Documentation/BILLING_SPECIALIST_STATUS.md` (note: that repo now has its own 3_Documentation/). Phase II (Odoo bank-watch) + re-nag cadence deferred by design.
