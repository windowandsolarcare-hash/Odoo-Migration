---
name: project_tech_app_architecture
description: "The fresh Tech App (static/tech/app.html) — auth model, TECH_GRANTED_OWNER scoped grant, money wrapper, care seam, what's held"
metadata: 
  node_type: memory
  type: project
  originSessionId: fd3d7991-aec7-45dc-97e5-4f403efbe28b
  modified: 2026-09-15T15:19:18.562Z
---

The **Tech App** is a fresh, cohesive sequential field app for technicians (built 2026-09-14, DJ-authorized; brief `3_Documentation/TECH_APP_BUILD_BRIEF.md`). Built from scratch (NOT v2_field with rooms removed) so it doesn't feel bolted-on; it **reuses the proven owner ENDPOINTS**, not the owner screens.

**★ WALKTHROUGH v2 (2026-09-15, ~35 DJ changes from a dry-run recording — all shipped, Lead-QC'd, doc `3_Documentation/TECH_APP_WALKTHROUGH_CHANGES.md`).** Stage order is now **Start-of-day → Job → ETA(Message) → At-the-door → Assessment → Do-the-work → End-of-job → End-of-day** (ETA moved to right-after-Job = "on the job you're driving TO", per H1). Key additions: Schedule = Today/Tomorrow/Week tabs + rich Command-Center-style cards (job-type edge color + service/freq/TOS/$amount/gate/paid/tags — info only, NOT owner scheduling controls) with times (added `time_pt`+`today` to api_upcoming); non-sticky nav (scroll the whole stage to reach Next); tappable stage pills; header pills (🗓 Schedule + 📖 SOP full-screen overlay) + active customer name; live-incrementing job timer (localStorage-persisted start); E4 work checklist incl "🚪 close the gate"; BEFORE photo → Assessment / AFTER → Do-the-work (pair posts on Save; `_eBefore` persists per-job); "Advance to the next job" resolves next from TODAY's schedule (NOT the retired Workiz next_job_link — removed from tech + from TECH_GRANTED_OWNER) → Maps + opens next Job card; End-of-day shift summary + read-only Approve (can't edit → call DJ) + back-to-schedule. Bugs fixed: "Other" pill (default no-selection), Cash NameError (see [[project_stale_so_payment_execute_payment_import]]). ALL under [[project_tech_app_collect_only_test_mode]].

**★ MONEY endpoints added (2026-09-15, each test-gated + canonical-reuse):** F4 tip = `POST /tech/api/job/add_tip` → adds "Tip" product #2 as a line via `save_job_lines_core` so invoice=base+tip (matches the check; no-stack rebuild). F5 Zelle/Venmo = `POST /tech/api/zelle_request` → delegates to extracted `specialist_billing._zelle_request_core` (owner route now thin) → texts the customer the canonical pay-request (paywatch arms); tapping Zelle/Venmo now REQUESTS (not record-as-received). F3 = client second-confirm on cash. Credit = `/tech/api/carddoor/*` (see credit paragraph). **Owner PRICE EDITOR** (DJ's single price access point, never-send-to-Odoo): `static/owner/v2_prices.html` (launcher "💵 Service Prices") + `routers/owner/prices.py` (`GET /owner/api/products/list`, `POST /owner/api/products/set_price` — non-negative+$100k cap, company[1,False]-guarded write of product.product.list_price). Care-repoint to product list_price is DESIGNED + blessed but HELD for DJ's issue→product map confirm.

**★ PWA INSTALL as "AT" (2026-09-15):** the tech app installs to the home screen as **"AT"** (distinct from the owner "WSC" app — owner manifest short_name is **"WSC"**, at `/static/manifest.json`; cheryl is "Cheryl — WSC"). Tech has its OWN `static/tech/manifest.json` with a **distinct `"id":"/tech/"`** — that id is what stops Chrome collapsing it into the WSC app (same-origin). `short_name:"AT"`, own icons `static/tech/icon-192.png`/`icon-512.png` (blue "AT" tile). ★ `start_url` = **`/static/tech/app.html`** (the PUBLIC shell, 200 cold), NOT `/tech/` (which 401s unauthenticated — a cold-open blocker). ★ `scope:"/static/tech/"` (NOT "/" — a "/" scope == the owner app's effective scope, so Chrome ASSOCIATES the page with the already-installed WSC app instead of offering a separate AT install; narrowing the scope is what makes AT installable side-by-side). Because scope is narrowed, the tech shell must NOT navigate out of scope: **sign-in is INLINE** in app.html — a name+PIN form that POSTs `/api/login` via **fetch** (fetch/XHR don't leave PWA scope; only navigations do) → stashes `tc_user` → `location.reload()` in-place. `toLogin()` + every 401 now SHOW that inline form (no `location.replace('/')` anywhere — that old root-nav broke standalone). app.html head must link THIS manifest (the bug: it linked `/static/manifest.json` = owner → only ever installed as WSC) + `<meta name="apple-mobile-web-app-title" content="AT">` + apple-touch-icon. Owner manifest LEFT UNTOUCHED (no id→defaults "/", scope "/", WSC) — distinct enough from tech; don't edit his live install. INSTALL: open the URL in a PLAIN Chrome tab (not from inside an existing same-origin PWA) → Add to Home screen.
- ★ **Same-origin SESSION COOKIE limitation (2026-09-15):** the session is ONE cookie `wsc_session` at `path='/'` (authz.set_session_cookie). On the SAME device+origin, owner (Dan/WSC) and tech (Danny/AT) SHARE it → the second login OVERWRITES the first (last-login-wins; can't be signed into both as different users simultaneously). Collect-only prevents data harm. Path-scoping is NOT a clean fix (the tech app uses granted `/owner/*` endpoints a `/tech`-scoped cookie wouldn't reach). Production non-issue (separate people/devices); only bites DJ's own same-device dual-test. True fix = a distinct cookie NAME per app (deeper auth change, not built).

**★ SCHEDULE SORT = Pacific time-of-day, NOT server UTC date_order (2026-09-15):** `/owner/api/upcoming` orders by `date_order` (UTC) and groups by UTC date. A Personal Time block at `00:00 UTC` is 5 PM PT the prior evening → sorts FIRST and shows in the wrong PT day ("5 PM then jumps to 8:30 AM" — DJ's live bug). The tech schedule sorts client-side by PT time-of-day (parse `time_pt`), for both the rendered cards and the underlying `jobs` array (so advance-to-next matches). Deeper fix (group by PT date server-side) not done — flagged.

Walks the SOP day in order: Start-of-day → Job card → At-the-door → **Assessment** → Do-the-work → End-of-job → Comms → End-of-day.

**★ Auth model (rediscovered the hard way — get this right):**
- Canonical entry = **`/static/tech/app.html`** (PUBLIC via the `/static` prefix — "UI shells, no data"), NOT `/tech/app` (that protected route was removed; the middleware 401s it before the handler = dead-end).
- ★ **`auth.py` route_map `"tech"` → `/static/tech/app.html`** (fixed 2026-09-15 — was `"/tech/"`, the OLD bolted-on hub `routers/tech/jobs.py`, which sent Danny to the wrong app after login). Post-login the session cookie is set so the shell's `/tech/api/*` calls are authed. The old `/tech/` hub route is still registered (legacy; retire later on DJ's call) — just don't route login to it. This is also the AT PWA `start_url`, so both the browser sign-in and the AT cold-open→sign-in land on the new app.
- Real front door = the existing **`/login`** (name + PIN → signed **tech-role session cookie**; it stashes `tc_user`={type,name,employeeId} in localStorage). There is NO in-page PIN gate and NO "access_code alone authenticates" — the session cookie is the auth. The app boots by reading `tc_user` (session proxy); absent or any API 401 → redirect to `/login`. `whoami` needs both a session AND an access_code and returns `{user:{employeeId,...}}` — the app skips it, using `tc_user` for identity.

**★ tech role is confined to `/tech` only** (`authz._role_allowed`). Reusing owner endpoints therefore needs a scoped grant: **`TECH_GRANTED_OWNER`** in authz.py (path-boundary matched, mirrors CHERYL_GRANTED_OWNER) — granted exactly: `/owner/api/upcoming`, `/next_job_link`, `/job/append_note`, `/maintenance`, `/attachment`, own clock (`payroll/status`,`clockin_crew`,`clockout_crew`,`break`), job timer (`timer/start`,`timer/stop`,`timer/sessions`). ★ List payroll/timer as EXACT sub-paths, never the greedy parent (payroll-admin + timer records/edit stay owner-only). MONEY is deliberately OFF the grant.

**★ Money** = the ONE tech endpoint **`POST /tech/api/record_payment`** (`routers/tech/payments.py`) — maps method→(method,memo) and calls the canonical `routers.owner.payments._stale_so_payment → _execute_payment` (DRY, no dup logic); amount read server-side from the SO. Tech role never touches an `/owner` payment route.

**Care seam** = [[project_wsc_busy_async_feedback_component]]'s sibling `routers/care_store.py` (shared, Specialists owns): Assessment findings (`POST /tech/api/care/item`) + before/after (`POST /tech/api/care/beforeafter`) → the customer PORTAL displays them. partner_id derived server-side from the SO. Photos reuse `POST /owner/api/attachment` (description 'care'/'beforeafter'). Window SOP embedded in Do-the-work via `/tech/sop` + `/tech/sop_pro` (serve the same static/cheryl/sop_windows*.html source, raw). `close_item`/`reopen_item` retract a finding (soft, status='closed'). **Prices** resolve at read time from DJ-editable `wsc.care.prices` (ir.config_parameter, by issue) → `price`/`price_note` on the item (null → portal "Price TBD"); never hard-coded; `get_prices`/`set_price` for a fast-follow owner editor.

**★ COLLECT-ONLY TEST MODE (SHIPPED 2026-09-15, DJ urgent — runs the app on REAL jobs in PARALLEL with his real ERP):** global server flag **`wsc.tech.test_mode`** in `routers/tech/testmode.py` — **DEFAULT-SAFE ON** (env `TECH_TEST_MODE` default `'1'`; and unset-in-Odoo also defaults ON — see the [[project_odoo_get_param_returns_false_unset]] trap: get_param returns bool `False` when unset, so False/None/'' → default ON). `is_test_mode()`, `log_intent(action,so_id,payload,preview)` → capped (200) `wsc.tech.test_log`. Routes: `GET+POST /tech/api/testmode`, `GET /tech/api/testlog` (DJ's review of collected intents). When ON, EVERY mutating tech action is collect-only: **payments.py** logs intent (server-read amount), no `_stale_so_payment`/`_execute_payment`/invoice/mark-done/next-SO; **job.py** POST lines logs a price-locked preview, no `save_job_lines_core`; **care.py** item+before/after → `test_log` NOT care_store (this was the exact overnight-leak vector); **comms.py** forces preview (builds text, sends nothing), logs only on confirm. **Belt-and-suspenders:** server logs-not-executes even if the client misbehaves, AND `static/tech/app.html` also client-no-ops the real-write vectors the server can't gate — the **photo upload skips `/owner/api/attachment`** (so no test attachment lands on a real SO), plus timer start/stop + clock in/out no-op. app.html has an ALWAYS-visible banner (🧪 TEST amber / 🔴 LIVE red) + a toggle w/ confirm + boot fetch of `/testmode`. Credit-at-door (#2) + tech price-edit (#3, DJ reversed the earlier no-price ruling) ride on this next.

**★ CREDIT-AT-DOOR in the tech app (SHIPPED 2026-09-15, rides on collect-only):** the tech is the inside guy with the card; he opens the SAME card-only page DJ uses (`routers/owner/carddoor.py`) — SINGLE SOURCE, no duplicated page. Mechanism: `api_carddoor_page` gained an `api_base` param (default `/owner/api/carddoor`, allowlist-guarded to only owner/tech prefixes) + a `__APIBASE__` token; the page JS sets `var AB=__APIBASE__` and posts to `AB+'/intent|/record|/receipt'`. The owner-rendered page is byte-identical-behavior (AB resolves to the old literal). `routers/tech/carddoor.py` = thin /tech wrappers (`/tech/api/carddoor/page|intent|record|receipt`) that add `so_on_schedule` + COLLECT-ONLY gating and delegate to EXTRACTED cores `_carddoor_intent_core`/`_carddoor_record_core`/`_carddoor_receipt_core` (owner routes are now thin wrappers over those same cores; `/record` → canonical idempotent `_stripe_record_and_close`). Money stays OFF `TECH_GRANTED_OWNER` (wrappers, not a grant). Test-mode: THREE layers — client Credit button doesn't open the page in test (logs 'credit_intent'), and `/intent` + `/record` (+receipt) each refuse+log when `is_test_mode()` → no real charge during DJ's parallel test. ★ Wallet/Google-Pay suppression is STRUCTURAL, not a flag: the page renders the combined Card Element (`_els.create('card')`) + `confirmCardPayment`, with NO PaymentElement/paymentRequestButton/expressCheckout element (so no wallet button can render), plus the intent is `payment_method_types[0]='card'`. A `wallets:{googlePay:'never'}` config does NOT apply to the Card Element and would require PaymentElement (a regression). This IS DJ's page incl. the 2026-09-08 revert to the combined element that killed the Google-Pay autofill he'd seen on Android.

**Comms/ETA (SHIPPED 2026-09-14, DJ-approved):** `POST /tech/api/eta` (`routers/tech/comms.py`) — FIXED option set only (ontime/m15/m30/m45/m60), preview→send, NO free-text; calls canonical `routers.owner.sms._eta_send` (extracted core; `api_eta` is now a thin wrapper, owner path byte-identical) with `company=True` → text reads "it's Window & Solar Care" (no doubled tail). Tech stays off `/owner/api/eta`. The "arriving around <time>" clock option is DEFERRED (needs the Google Routes API enabled for GOOGLE_API_KEY). **Still deferred:** inside/outside mode toggle (v1 single-mode); a tech seeing only his ASSIGNED jobs (v1 shows the whole day). **Tech app is v1 feature-complete: Start-of-day→Job card→At-the-door→Assessment→Do-the-work→End-of-job→Comms→End-of-day.**
