---
name: project_paywatch_specialist
description: "Billing Phase II LIVE 2026-07-27: routers/owner/specialist_paywatch.py watches Gmail (GMAIL_WSC_APP_PASSWORD, now SET) for Venmo/Zelle payment-notification emails, matches to wsc.billing.awaiting.* by NAME (not exact amount), surfaces an approval card 'X paid $Y — record + mark Done?' → records via _stale_so_payment + settle. Armed-by-work; runs every 15 min via main.py scheduler. Verified end-to-end on 3 real payments."
metadata:
  node_type: memory
  type: project
  originSessionId: 44fd339a-14fd-42e2-adef-f1ae97081400
  modified: 2026-07-28T06:55:10.726Z
---

**2026-07-27 — DJ's idea, lead-speced, built.** `routers/owner/specialist_paywatch.py` (registered main.py). Closes the billing loop from Venmo/Zelle notification EMAILS (faster than bank lines). Stripe already covered by the settle hook on api_stripe_success — this is Venmo + Zelle only.

**Verified parse formats (real emails DJ got 2026-07-27):**
- **Venmo** — sender `venmo@venmo.com`, subject `"<Payer> paid you $<amt>"` (parse from SUBJECT), memo in body.
- **Zelle** — sender `no.reply.alerts@chase.com`, subject "You received money with Zelle®"; BODY (HTML) has `<h1><NAME> sent you money</h1>` + table rows `Amount $X` / `Transaction number` / `Memo`. Strip tags, regex `(.+?) sent you money`, `Amount \$([\d,.]+)`, `Memo (.+?) <Name> is registered`.

**★ MATCH ON NAME, not exact amount** (Kristin was awaiting $200 but PAID $245 via Venmo — awaiting amount is DJ's estimate; the email amount is truth). Payer ≠ customer sometimes (Patti Martel paid for Art Martel's job) → fuzzy shared-token (last-name) match; show the actual amount, flag if it differs from expected. **ARMED-BY-WORK:** `run_paywatch_tick()` returns immediately if no `wsc.billing.awaiting.*` (never touches Gmail); "I sent it" arms, approval disarms. **CLAIM not money:** never auto-records; every match → interrupt approval card `paywatch:<so_id>` → `POST /api/paywatch/record` runs `dashboard._stale_so_payment(so_id,'cash','Zelle'|'Venmo')` (right bucket) then `billing.settle()`. Dedupe by msgid in `wsc.paywatch.processed`. Endpoints: `GET /api/paywatch/scan` (read-only verify), `POST /api/paywatch/tick`, `/record`.

**✅ LIVE 2026-07-27 (was blocked, now set + verified end-to-end).** DJ created a "wsc watcher" Gmail App Password; set on Render `GMAIL_WSC_APP_PASSWORD` via `mcp__render__update_environment_variables` (MERGE, replace=false — never wipes other keys). `/api/paywatch/scan` then matched all 3 real payments: Zelle $150 PATTI MARTEL→Art Martel (exact), Zelle $200 NICHOLAS CONWAY→Nick Conway (exact), Venmo $245 Kristin Acker→Kristin (flagged vs $200 expected). Fired tick → 3 approval cards live. Scheduled `_scheduled_paywatch` every 15 min in main.py (armed-by-work). Zelle payer regex captured a stray "payment " lead-in → stripped. Card TITLE uses the matched CUSTOMER name; payer/memo in why_now. DJ's app-pw screenshot is in his Evernote folder — he can delete it now. HISTORICAL: it had returned "GMAIL_WSC_APP_PASSWORD not set" (armed correctly on 4 awaiting — logic works). Env var uses the SAME name as `vault._pull_email_docs` (L511) and `myday` poller (L1354) — so it's genuinely unset on Render, meaning Vault email-import + My Day email features are ALSO dead. NEXT: DJ creates a Gmail App Password (Google Account → 2FA → App Passwords) for windowandsolarcare@gmail.com, drops it in a FILE ([[feedback_api_keys_via_file]]), and it's set on Render via fetch+merge+PUT ([[feedback_render_put_env_vars]] — never wipe). Also still to wire: a ~10-15min in-app scheduler tick calling run_paywatch_tick (armed-by-work makes extra runs ~free). See [[project_billing_specialist]], [[project_agent_mail_channel]].

**★ DUPLICATE-CARD / ORPHAN FIX (DJ 2026-07-27, screenshot):** DJ saw TWO cards per customer in the HUD — a `billing:<so_name>:waiting` tracker ("Awaiting $200 — Kristin", created by `_mark_asked` on "I sent it", kind=attention/urgency=glance, passive "Open job" tap) AND the later `paywatch:<so_id>` approval ("Kristin paid $245 — record"). He worried every send would permanently orphan a waiting card. Root cause: the waiting tracker was only cleared by `settle()` on APPROVE — so during the whole sent→email→approve window BOTH cards coexisted, and the waiting one looked dead (glance card, easy to think it's un-tappable). Fix: `_submit_card` (paywatch) now calls `delete_item('billing:%s:waiting' % a['so_name'])` right after submitting the approval card — collapses the pair into ONE card at the moment money lands ("Awaiting" → "paid, record"). On approve, `api_paywatch_record` still deletes `paywatch:<so_id>` (L300) + `settle()` clears the waiting card + awaiting param (backstop). So lifecycle = ONE card throughout: Awaiting (waiting for money) → Paid-record (email arrived) → gone (approved). Deployed commit aa0b870. Also manually removed the 3 stale duplicate waiting cards (Kristin 004577, Nick 004766, Art 004909) from `wsc.feed.items` since their paywatch cards already existed; Jose 004510 waiting card kept (stripe, no payment email yet). NOTE for lead: the waiting card is `kind:'attention'` so it lands in "Needs you" and inflates that count even though it needs no action — consider routing glance-urgency passive trackers to "Info" instead (lead owns HUD tab routing).

**RECORD-AMOUNT FIX (lead review, DJ 2026-07-27):** api_paywatch_record records the ACTUAL amount that landed via `dashboard._execute_payment(so_id, amount, 'cash', memo)` — NOT `_stale_so_payment` (which forced amount = SO.amount_total, silently losing overpayment: Kristin sent $245 vs a $200 estimate). The on_approve payload carries the real amount; the endpoint uses it. _execute_payment also has no tech-gate. Card copy DE-PROMISED: it does NOT mark the job Done or fire Phase 5/6 (recording payment != Workiz Done); copy = "record this $X payment?" + body reminds DJ to mark Done in Workiz himself. Follow-up (Phase II.b, not built): auto-mark-Done would need a Workiz status write.
