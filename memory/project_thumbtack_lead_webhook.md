---
name: project_thumbtack_lead_webhook
description: "Thumbtack lead webhook → live app. Phase 1 (log-first endpoint) SHIPPED; Phase 2 (parse→crm.lead→surface) PENDING DJ's Test lead. Endpoint/secret/raw-log facts."
metadata: 
  node_type: memory
  type: project
  originSessionId: fd3d7991-aec7-45dc-97e5-4f403efbe28b
  modified: 2026-09-11T00:58:00.309Z
---

**Why:** Workiz died 2026-08-03 but Thumbtack still pushes DJ's leads to that dead integration (most of his business is Thumbtack) → leads bleeding into a hole. Fix = capture leads via Thumbtack's native webhook (Thumbtack → Apps → Webhooks → Create webhook; **Leads only**, one-way, additive — no need to remove the dead Workiz first). Full brief: `3_Documentation/THUMBTACK_WEBHOOK_BRIEF.md`. Lead payload has customer NAME + PHONE only (NO email — Thumbtack policy) → match res.partner on PHONE.

**Phase 1 — LOG-FIRST (SHIPPED 2026-09-10, tips 66f5418+e8bc43a):** new file **`routers/thumbtack.py`**, registered in main.py with NO prefix → `POST /webhooks/thumbtack/<secret>`. PUBLIC by design (Thumbtack can't hold a login cookie) — the path is OUTSIDE authz PROTECTED_PREFIXES ('/owner','/tech','/cheryl') so the gate lets it through, and it's feature-namespaced (route-shadow rule). The `<secret>` segment IS the auth: must equal ir.config_parameter **`wsc.thumbtack.webhook_secret`** (40-char, stored there + local `/c/Users/dj/_thumbtack_secret.txt`; full URL in `/c/Users/dj/_thumbtack_url.txt`). Wrong/missing secret → 404 (endpoint hidden). Returns 200 fast (Thumbtack retries any non-200). It LOGS the raw payload to a ring buffer **`wsc.thumbtack.raw_log`** (ir.config_parameter, last 30, readable via Odoo RPC) + print()s — creates NO records yet. Test-lead detection = `_looks_like_test` (best-effort until the real shape is known). VERIFIED live end-to-end (correct secret→200, test:true; wrong→404; payload captured). ★ Secret/URL is NOT in AGENT_MAIL (that commits to GitHub) — relay to DJ via direct message/file only.

**REAL payload shape (DJ's Test lead 2026-09-11):** a **NegotiationCreatedV4** event — `{event:{eventType,triggeredAt,webhookID}, data:{negotiationID, customer:{customerID,firstName,lastName,phone}, business:{businessID,name}, request:{requestID,description,category:{name},location:{address1,address2,city,state,zipCode},details:[{question,answer}],proposedTimes,attachments}, estimate:{total,pricePerUnit}, status, leadPrice, chargeState}}`. **NO email** (match by phone). **NO explicit "test" flag** — Thumbtack's Test lead uses `business.name`="Test Business for Webhooks" + phone **1234567890** — THOSE are the test markers. ★ CRITICAL (Lead QC 2026-09-11, tip ff26aca): do NOT key `_is_test` on `businessID` — **423802860942737432 is DJ's REAL W&SC business id** (his webhook businessPk + public profile), so a REAL lead carries it too; keying on it skipped every real lead. `_is_test` now uses name/phone/substring only. Re-verified live: real lead w/ businessID 423802860942737432 + name 'Window & Solar Care' → CREATES; Thumbtack Test → skips.

**Phase 2 — SHIPPED 2026-09-11 (tip 94762e5), VERIFIED end-to-end:** in `thumbtack_webhook` after the log: act ONLY on `eventType` startswith "NegotiationCreated" (ignore updates/messages); `_is_test` skip (logged, no record); idempotency on `data.negotiationID` via ir.config_parameter **`wsc.thumbtack.seen_negids`** (redelivery → "already processed", no dupe); `_match_partner` by PHONE (digit-normalized, last-7 `like` prefilter then last-10 exact, `company_id in [1,False]`) else create res.partner (company 1); create **crm.lead** (company 1, type='lead', tag "Thumbtack" crm.tag id 19, partner_id linked, name "Thumbtack: <category> — <name>", description = category/request/location/estimate/leadPrice/Q&A/negotiationID); bidirectional partner note; **surface = `notify.push_dj(sms='auto')`** (banner + away-SMS — the "DJ SEES it" per [[feedback_never_send_dj_to_odoo]]). Returns 200 fast, fully try-wrapped. Verified: Test→skipped; real synthetic→crm.lead+partner (company 1); resend→idempotent (then cleaned up). ★ LESSON: a webhook test that calls push_dj(sms='auto') DOES text DJ if he's flagged away (a QC-artifact SMS went out) — stub notify or check presence before testing notify paths. Surfacing beyond the banner (a My Day task / reactivation-inbox card) is an easy add if Lead wants it. Workiz-integration removal on Thumbtack is separate/low-pri (webhook is additive → not a blocker). See [[feedback_bidirectional_creation_links]], [[project_new_order_parked_surfacing]].

**Lead REPLY flow (DJ-requested, SHIPPED 2026-09-12, thumbtack.py 81743ec + sms.py 97fd43e):** on a new lead, thumbtack.py drafts the first text via `sms._draft_outbound(pid, intent)` (Dan's voice; intent REQUIRES the opener "Hi <first>, this is Dan with Window & Solar Care — thanks for reaching out on Thumbtack about your <service>" since it's a cold SMS from an unknown number; no price/date) and creates a `feed.submit_item` LEAD ACTION card (kind='approval'): summary + editable draft + Approve/Edit/Send + a one-tap "Respond in Thumbtack" action (https://www.thumbtack.com/pro/messages) — REPLACING the old generic needs-you Q&A banner that confused DJ. Plus a `push_dj(tap_only=True, sms='auto', action_href=/static/owner/v2_home.html)` away-nudge (navigates, not a reply modal). **SEND path = `POST /owner/api/thumbtack/reply`** (in sms.py, OWNER-AUTHED — the webhook router is public so the send endpoint must live in an /owner router) → `messaging.send(to=phone, body=edited, kind='thumbtack', idem=...)` (WSC signature auto-appends), clears the card. APPROVAL-GATED, never auto-sends.
★ CHANNEL FINDING (verified): the lead phone is the customer's REAL cell (not a TT relay) → a text is a DIRECT SMS to them, NOT through Thumbtack, so it does NOT register as a TT in-platform response (response-time badge) — hence the companion "Respond in Thumbtack" link. The $61.42 lead fee is triggered on lead creation/match, not by the reply. ★ TEST LESSON (again): firing a synthetic lead runs push_dj(sms='auto') → texts DJ if away; touch wsc.dj.last_seen=now before testing notify paths, and clean up the crm.lead + res.partner + wsc.feed.items card + seen_negids + dj_alerts after.
