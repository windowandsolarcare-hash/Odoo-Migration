---
name: project_zero_downtime_worker
description: "W&SC app zero-downtime plan (DJ approved 2026-09-22, Builder-2 leads) — extract the 18 cron jobs to a single-instance Render Background Worker so the web tier can go numInstances=2 without double-firing money/customer crons. CODE built + held for Lead HARD QC (money app). + the module-level import-os boot-crash catch."
metadata: 
  node_type: memory
  type: project
  originSessionId: 93ae5c9a-b2db-49a9-8fa8-84d13000c2ae
  modified: 2026-09-23T14:36:20.914Z
---

**Goal:** true zero-downtime deploys — a deploy/hiccup never 502s the field tech or a customer payment. Spec: `saunders-render-app/3_Documentation/ZERO_DOWNTIME_SPEC.md`. Root cause of the 2026-09-22 502s = single web instance restarting on deploy (no healthy target). Fix = web numInstances≥2, but that would DOUBLE-FIRE the 18 in-process crons (reminders/paywatch/reengage = money/customer) → must first make the scheduler single-instance.

**Design (extract-to-worker, chosen over leader-lock):** main.py's `lifespan` boots an `AsyncIOScheduler` with 18 `_scheduled_*` jobs (line ~648). Refactor: extract the job list into a module-level **`build_scheduler()`** (funcs unchanged); the web lifespan runs it behind a **★ DEFAULT-ON gate** (Lead constraint #1, money-safety): `os.environ.get('RUN_SCHEDULER','true').strip().lower() not in ('false','0','no','off')` — so a code-only deploy with NO env change KEEPS crons running on web exactly as today (step-1 deploy = a true no-op; crons never go dark by a deploy; no missed/double sends). New **`worker.py`** (`python worker.py`) imports build_scheduler from main + runs the scheduler on its own asyncio loop UNCONDITIONALLY (it's the sole owner; ignores RUN_SCHEDULER — that env is only the WEB toggle); `import main` runs module code + lazy Sentry but NOT the lifespan → no auto-start. Invariant AFTER the flip: **web = RUN_SCHEDULER=false → ZERO scheduler → safe at N instances; worker = the ONLY scheduler (numInstances=1) → no double customer texts.** + lazy Sentry (main + worker), NO-OP until SENTRY_DSN set (DJ), requirements += sentry-sdk>=2.0,<3.0 + psycopg-pool==3.3.3 (Specialists' inbox-pool dep, reserved in the same held commit). Force-tested: unset/true/1/yes→starts, false/0/no/off→no-start (constraint #1 proven).

**★★ BOOT-CRASH CATCH (reusable):** in this app `os` was imported ONLY LOCALLY inside functions (`import os` at lines 256/289/479/536), NOT at module scope. A module-level or lifespan reference to `os.environ` (my gate + Sentry block) would `NameError` at boot = money-app crash. **`python -m py_compile` does NOT catch this (it's name-resolution, not syntax).** Fix: added module-level `import os`; verify with AST that a needed import is at module scope, don't trust py_compile for name resolution. Always check that names used at module level are imported at module level.

**✅ STEP-1 DEPLOYED + MONEY-VERIFIED (2026-09-23, commit b4478cde, deploy dep-dapu6erm8hqs73apf77g, Lead-signed-off).** One commit (main.py + worker.py + requirements) landed via the **Git Data API** (blobs→tree→commit→ref, force:false compare-and-swap on head) = ONE deploy/restart (the main-ruleset ALLOWS a git-data ref update, not just Contents PUT — useful for atomic multi-file commits). ★ DRIFT CAUGHT at push: live main.py had grown 917→932 (+15 lines from other sessions since my hours-earlier fetch); pushing my stale edited copy would've reverted their work → instead re-applied my 3 changes onto FRESH live via the transform + a NORMALIZED diff (both `tr -d '\r'` — Python's Windows text-write adds CRLF, which makes a raw diff show the whole file changed; always normalize before diffing/pushing, and push LF). Live boot confirmed `[scheduler] STARTED in-process (RUN_SCHEDULER default-on)` (constraint #1: crons kept running on web — no missed/double sends) + clean install + no ImportError + Sentry lazy no-op. numInstances=2 prerequisite now COMPLETE. **STEP-2 FLIP remains DJ-gated** (worker service + SENTRY_DSN + numInstances=2 via the runbook), top of DJ's return queue.
**★ DEPLOY (DEFAULT-ON simplifies it):** STEP-1 = push main.py+worker.py+requirements with NO env change → web keeps running the scheduler (default-on) = a true no-op, but it's still a real single-instance RESTART (blip), so it goes through a Dispatcher money-check + a QUIET window + a post-deploy "crons still fire" verification (recommended: bundle with the reserved requirements commit = one blip, one check). STEP-2 FLIP (DJ-gated, runbook `3_Documentation/review/zero_downtime/STEP2_FLIP_RUNBOOK.md`, GAP-over-DOUBLE order): (1) set web RUN_SCHEDULER=false FIRST → brief no-scheduler GAP (safe; misfire_grace covers a missed tick); (2) bring up the Background Worker (`python worker.py`, numInstances=1, copy web env + SENTRY_DSN) → crons resume there, gap ends; (3) verify exactly ONE scheduler (18 jobs in WORKER logs), web has none; (4) web numInstances=2 + fast /healthz + graceful shutdown. NEVER two schedulers at once (double customer send). Rollback: web numInstances=1 → web RUN_SCHEDULER=true → delete worker.

**BLOCKERS/flags:** Render MCP has create_web_service but NO create-background-worker → the worker service needs DJ (dashboard New Background Worker, same repo, `python worker.py`, RUN_SCHEDULER=1) or a render.yaml. SENTRY_DSN = DJ. **STATUS: CODE built + compiled, HELD for Lead HARD QC before ANY money-app deploy; nothing deployed.** Lead QC gate: the double-fire test (web@2 + a reminder fires EXACTLY once). Coordinate the flip window (not while DJ transacts). Local working files: C:\Users\dj\wsc_main_live.py, wsc_worker.py, wsc_req.txt. See [[feedback_compile_gate_before_push]] (chain compile→push) + [[feedback_no_deploy_during_customer_payment]].
