---
name: project_hud_followups_surface
description: "HUD \"Follow-ups waiting on you\" surface (read-only open-loop / needs-action list) — routers/owner/followups.py, feed card outreach:followups, data in wsc.followups.data. NEVER sends to customers."
metadata: 
  node_type: memory
  type: project
  originSessionId: 4ab12b63-cc8f-44de-b410-58b38aa2a6c9
  modified: 2026-08-01T03:46:59.265Z
---

Shipped 2026-07-30 (lead lane). Surfaces the customer "open loops" (who's waiting on Dan) in the HUD.

**Pieces:**
- `routers/owner/followups.py` (NEW, isolated) → `GET /owner/api/followups/view` renders a grouped read-only HTML page (🔴 Needs action / 🟢 New leads / 💵 Billing / 🟡 Deferred / ⚪ Declined / 🛑 Opt-out). Also `GET /api/followups/data` (JSON). Registered in main.py (import + include_router prefix=/owner, after owner_outreach).
- Data = `ir.config_parameter` **`wsc.followups.data`** = JSON list of `{name, cat, do, last, phone}`. Refresh the list = overwrite this param (no redeploy). Built by `C:/Users/dj/build_followups.py` from the Workiz sweep (`C:/Users/dj/followups.json`).
- HUD feed card `outreach:followups` (kind attention, source outreach, urgency today, badge=needs-action count, action.href=/owner/api/followups/view) → shows in HUD "Needs you" tab. Submitted via POST /owner/api/feed/submit.

**SEND-ON-COMMAND (added 2026-07-30, DJ's design):** each actionable item now carries a `draft` (a suggested text I wrote in DJ's voice, `[brackets]`=fill-in). The page renders it in an EDITABLE textarea + a **Send via Twilio** button + a 📞 Call link. On tap: JS `sendFU()` → `POST /owner/api/followups/send {phone,body,name}` → **`messaging.send(phone, body, partner_id=, kind='followup')`** (the OTHER agent's canonical outbound layer, `routers/owner/messaging.py`; note in AGENT_MAIL: "Do NOT write another Twilio call"). That one function does it all: Twilio via the **A2P Messaging Service**, **STOP/opt-out** gate, **Do-Not-Contact** gate, **idempotency**, **in-app inbox threading** (shows in the customer's text chain, bumps inbox), and the **Odoo comms log** (partner + SO). Switched to it 2026-07-31 (was raw `sms._send_sms` + hand-rolled threading — that bypassed the STOP/DNC guards). Partner resolved by last-10 phone for the DNC check + logging. So the 4 STOP customers auto-block; no accidental resend.
**★ TWO HARD GATES so nothing sends prematurely (DJ: "nothing goes now just building"):** (1) the send endpoint refuses unless `ir.config_parameter` **`wsc.followups.send_enabled` == '1'** (default '0' = build mode; verified: tapping Send in build mode returns `{disabled:true, "Sending is OFF"}` and sends nothing). (2) even armed, `_send_sms` only fires if Twilio creds + the ported number are set on Render. **NEVER auto/bulk sends — one message, DJ's edited text, DJ's tap.**
**✅ LIVE as of 2026-07-31 (port night):** the Twilio port completed, TWILIO_* creds are set on Render, `wsc.followups.send_enabled = '1'` (armed), and DJ tapped Send on a real follow-up — it went out and threaded into the customer's chain. Send path verified end-to-end. Sending is NO LONGER in build mode — it is on. (Each send is still one message, DJ's edited text, DJ's tap — never auto/bulk.)

**To GO LIVE post-port (historical, now done):** confirm the number is ported onto Twilio + TWILIO_* env vars set on Render, then arm sending. **DJ can self-serve via a TOGGLE on the followups page** (added 2026-07-30): the banner shows "Build mode - sending is OFF [Turn ON]" / "Sending is ON [Turn OFF]"; the button → `POST /owner/api/followups/arm {enabled}` → sets `wsc.followups.send_enabled` '1'/'0' (confirm dialog first). Or `odoo set_param wsc.followups.send_enabled 1` directly. See [[feedback_email_draft_first_always]] (same principle: I draft, DJ sends) and [[feedback_ported_means_twilio]].

**AI-REWRITE (added 2026-07-31, DJ's request "give me access to Claude to re-write a message he already suggested"):** shared endpoint **`POST /owner/api/ai/rewrite`** in followups.py — body `{text, instruction}` → `{ok, text}` (or `{ok:false,error}`). Uses `CLAUDE_HAIKU` via `get_anthropic_client()`, rewrites the draft in Dan's voice, preserves facts, returns ONLY the message. **SENDS NOTHING** — regenerates draft text only; the send gate is untouched. Follow-up drafts have a "✨ Ask Claude to rewrite" button (`rewriteFU()` — prompts for an instruction, POSTs, replaces the textarea). Booking specialist (other lane) was mailed the endpoint + a ready-to-paste button for their reply cards (AGENT_MAIL 2026-07-31). **The SMS inbox (`static/owner/v2_inbox.html`) also uses it (added 2026-07-31): a "✨ Ask Claude to rewrite" button (`.rw-btn` / `rewriteReply()`) in the reply dock rewords the `#reply` box (falls back to `CUR.draft` if nothing typed) via the same endpoint.** One engine, three call sites (followups drafts, inbox reply, booking cards) — see [[feedback_reuse_canonical_endpoint]] and [[project_inbox_suggested_reply_drafter]]. (Note: the HUD card "Open ↗" button — v2_hud.html — is the only door to each card's full screen; DJ chose 2026-07-31 to LEAVE IT AS-IS, do not remove it.)

**REPLY-AWARE ACTIONS (2026-07-31, evolved same day — DJ: "can Claude analyze my response and do something from it… launch new job or present me a button").** When Dan has replied to a follow-up customer (his message is the last in the `wsc.sms.conv.{norm}` thread, `last_dir=='out'`), `_apply_replies(data)` reads his reply and **classifies it via Claude Haiku** (`_classify_reply`) into one of three, then acts:
- **done** (just answered / gave a price / thanked) → REMOVE from list (the original "I replied, drop it" behavior).
- **schedule** (committed to booking/coming out — "I'll get you on the schedule", "I'll come Thursday") → KEEP as an ACTION card: `do` line becomes "You told them you'd get them on the schedule — book it:" + a **"🗓️ Get them on the schedule"** button linking `/static/owner/v2_new_order.html?contact=<pid>&name=<n>` (pid via `_resolve_pid`, company_id in [1,False]). That deep-link runs the FULL New Order logic (parked-campaign check → `v2_new_job.html?contact=<pid>` prefilled). **v2_new_order.html was given a prefill hook 2026-07-31** to accept `?contact=`/`?partner_id=` and auto-run `pickCustomer`. DJ's choice: "button now, auto later" (Dan taps; no auto-create) + button must BE the New Order logic (reuse, not a 2nd booker — [[feedback_reuse_canonical_endpoint]]).
- **followup** (circle back later — "I'll check back in the fall") → KEEP, `do` becomes "You said you'd circle back with them." + Call. (v1 = keep-visible only; dated-reminder is a fast-follow, not built.)

Classification is **cached** on the item (`_act` + `_act_src`=the reply text) so Haiku runs once per reply, not every render. If the customer texts AGAIN (`last_dir` back to 'in'), `_act` is cleared → normal open loop. `_apply_replies` runs at the top of `followups_view` and on successful `followups_send`; persists `wsc.followups.data`. Degrades safe: no AI → 'done' (old remove). Because every outbound (follow-up screen, inbox, reminders — all messaging.send) threads into the same conv, it catches "out of follow-up" replies too. Verified 2026-07-31: Tim Albert's reply ended "I'll get you on the schedule?" → classified `schedule`, pid 27140, card shows the New Order button. Intents live = **schedule + follow-up** (DJ's pick); "we could develop many more" (estimate, etc.) is the extension path. Caveat: an automated outbound could flip last_dir→out and trigger classification; low risk for these manual loops.

**Current list = a SNAPSHOT** (as of 2026-07-30, from the one-time Workiz text sweep — [[project_workiz_chat_export_to_odoo]]). It does NOT auto-refresh yet: the open-loop detection ran over the exported transcripts (last-message speaker + content heuristics, dropping outreach-blasts/auto-confirms/spam/STOP). **TODO to make it living:** after the Twilio port, new inbound/outbound texts land in Odoo per-partner (comms log) → re-run the open-loop classifier server-side on a tick and rewrite `wsc.followups.data` + refresh the card. Until then, regenerate manually from a fresh sweep.

**How to apply:** to update the list, edit/rebuild `followups.json` → `odoo set_param wsc.followups.data <json>` → re-POST the feed card (or just let the existing card stand; it re-reads data live). To add columns/actions, edit followups.py — but keep it READ-ONLY (compose-only, never send).
