---
name: project_vault_evernote_drive
description: "Planned 'Vault' = Evernote replacement in the owner app, Drive-primary (gigabytes of files live in the Saunders Vault Google Drive folder, NOT Odoo). Needs a durable Google auth (service account) first."
metadata: 
  node_type: memory
  type: project
  originSessionId: 43ecbb04-6bcc-42b6-aae3-303ecce01b59
---

**★ CURRENT STATE 2026-07-06 (supersedes the stale Phase-1/auth text below — DJ was right those were old):** Vault is FULLY built (note create/edit, photo+file upload, AI auto-file, folders, tags, customer links, to-do, search-inside-PDFs) AND **Drive auth is HEALTHY** (verified: 44 notes load, pin writes succeed — the ~weekly-expiry is effectively handled; don't re-chase unless notes actually fail to load). **UNIFIED NOTES+REFERENCES (DJ: "like Evernote, notes and docs the same, put a pin on each card"):** dropped the create-time "Note vs Pinned reference" tabs — ONE Add makes a note/doc, and a **📌 pin on every card** makes it a reference. Pin = Drive `properties.pinned='1'` (new `POST /api/notes/{id}/pin` in notes.py; `list_notes` returns `pinned`; `renderPinned` filters `_allNotes`). **Old Odoo Reference app RETIRED** — its 2 cards migrated to pinned Drive notes (Call Back Numbers; W&SC LLC CA SOS Filing text + 2 EIN PDFs uploaded & pinned), Odoo cards deleted (0 remain). **OFFLINE CACHE:** `cachePinned()` stores pinned refs {id,name,category,text} in localStorage each load; `renderPinnedFromCache()` shows them instantly + on no-signal (text via alert fallback). Commits notes.py 44207868 + vault.html 8b52c7a4. ⚠ gotcha: don't match notes by name substring to pin (accidentally pinned a pre-existing "EIN for Saunders Printing.pdf"; unpinned) — pin by exact id.

**TRUE OFFLINE PINNED FILES 2026-07-06:** DJ's core want for pinning = important stuff available with NO service. The earlier "offline cache" only stored pinned notes' TEXT (localStorage) — NOT the actual files/images, so a pinned PDF/image/folder (e.g. Kaiser card scans) still needed Drive. FIXED: backend `GET /api/vault/file?id=` downloads a Drive file's raw bytes (Google Docs exported→PDF). Frontend (`cachePinnedFiles` after `loadPinned`) downloads every pinned item's bytes into **IndexedDB** (`wscVault`/`files` store) — for a pinned FOLDER (multi-file note) it lists children + caches each (+ a folder manifest). `openPreview` is now **cache-first**: shows the local blob (works offline) else the Drive preview. Pinned folders open via `openCachedFolder` (offline children list). ★ DJ must open the Vault ONCE with signal to populate the cache; then pinned items open offline. Verified: /api/vault/file pulled a 1.75MB Kaiser scan + a 10KB EIN PDF. Commits vault.py 6c2a1804, vault.html caf9b8b4.

**WRAPPER FLATTEN DONE 2026-07-06:** the Evernote import wrapped every note in a dated folder ("YYYY-MM-DD Title" / "#### · Title") holding its Doc (+ any attachments). That made every note show as BOTH a folder and a doc → confusing to pin/browse. Flattened all **1,209 SINGLE-doc wrappers**: moved the Doc up into its stack, kept exact title, stamped `src_note` provenance property, trashed the empty wrapper (reversible ~30d). **Multi-file notes (doc + scans) KEEP their folder** (so doc+attachments stay together) — e.g. the Kaiser card = folder w/ doc + 2 img_*.jpg. ★ PIN MODEL: single-doc note → pin the DOC; multi-file note → pin the FOLDER (holds everything). Endpoint `/api/vault/wrapper_scan`: `?build=1` scans+caches work-list (slow, whole-Drive file list, 80-page cap); `?flatten=1&limit=N` time-budgeted (~70s, ~1.6s/wrapper) flatten from cache — re-call until remaining=0. ★ Drive rate-limits rapid folder moves → big batches 502/timeout; use small batches (limit 15) + resilient retry loop (ran ~210 iters, 139 transient errs, still finished). Reconciliation-SAFE: nothing deleted, docs moved not removed.

**DJ wants an Evernote replacement built into the owner app** (the stuff he'd normally put in Evernote — business docs, scans, account info, etc.). Stated 2026-06-27.

**STORAGE DECISION = DRIVE-PRIMARY (not Odoo).** DJ is talking **GIGABYTES** of files. So files must live in **Google Drive**, NOT in the app's Odoo backend (Odoo SaaS has storage quotas, bloats/slows, costs money — wrong tool for GBs). The app is the friendly front-end that uploads to / lists / opens from Drive; it does NOT keep duplicate copies. (Contrast: the Reference app's file attachments, built same session, ARE Odoo-backed — fine for a few small docs like the EIN PDFs, NOT for the bulk vault.) DJ picked "app-primary + mirror" at first but once GBs came up, Drive-primary is correct (he'd even floated "delete the app copy after upload" = same idea).

**The destination already exists:** Google Drive folder **"Saunders Vault"**, id **`1uVXJjg4YYfqcijh4Vbvf-snWGfUIyb9Q`** (My Drive root, owner windowandsolarcare@gmail.com, created 2026-05-18). Subfolders = "notebooks."

**FOUNDATION / AUTH — partially understood; NOT permanently solved (2026-06-28, corrected).** The live app talks to Drive via `notes.py` `_drive_service()` = USER OAuth (env `GOOGLE_OAUTH_CLIENT_ID` / `GOOGLE_OAUTH_CLIENT_SECRET` / `GOOGLE_OAUTH_REFRESH_TOKEN`, scope `.../auth/drive`). ★ A **SERVICE ACCOUNT IS CONFIRMED WRONG** for the always-on app: the SA (`render-drive@gen-lang-client-0790905441…`) **can create folders but CANNOT create Google Docs → 403 storageQuotaExceeded** (SAs have 0 My-Drive quota), tested 2026-06-11. Storage = 5 TB Google One on the **consumer** acct `windowandsolarcare@gmail.com` (gmail.com = Google One, NOT Workspace); SA+paid only works on a Workspace Shared Drive, and DJ's 1-user `dan@scenicartprint.com` Workspace can't hold the 5 TB. So consumer = OAuth, never SA. ⚠ **Publishing to "In production" did NOT cure the recurring expiry** — per [[project_notes_google_oauth]] the token died 6/11, 6/19, 6/22 (~weekly) EVEN while Audience shows "In production"; root cause is likely the **Drive scope is RESTRICTED + the app is UNVERIFIED** → tokens capped regardless of publish status. Real permanent fix = (a) Google verification of the app, (b) move Vault to a Workspace Shared Drive + SA, or (c) re-platform notes off Drive. (My earlier same-session "RESOLVED/stable, 0 errors in 14d" claim was WEAK — the 503s don't necessarily hit stdout logs, so absence of log matches ≠ token healthy. It happened to work when tested 6/28.) Manual re-mint ~weekly via `C:\Users\dj\mint_google_token.py` is the current stopgap. **NOTE — the one-time Evernote IMPORT does NOT use this token:** `process_enex_to_gdrive.py` runs LOCALLY with its own desktop OAuth flow (credentials.json + token.pickle), so the weekly-expiry problem does NOT block the import.

**Other gigabyte caveats:** (1) storage = **5 TB Google One on windowandsolarcare@gmail.com** (confirmed ample — DJ 2026-06-28). (2) Big uploads must go **chunked/resumable straight to Drive** so they don't choke Render or time out.

**VERIFIED DRIVE STATE 2026-06-28 (via SA read-only list of root `1uVXJjg4YYfqcijh4Vbvf-snWGfUIyb9Q`):** root has **28 folders** = the Evernote-stack skeleton (Auto/Real Estate/W&SC/Personal/Finances/Kaiser/Medical/Tax Documents/Printing/Cheryl/Mom and Dad/Important/etc.) + Quick Notes + Indeed. ★ **The notebook folders are EMPTY SHELLS** — drilled "Auto Stack/Toyota Tacoma 2022", "W&SC Stack/Workiz Scripts", "Real Estate Stack/Vigil Tomas" = 0 items each. **Only 2 notebooks actually have content: Accounting Stack (22 attachments) + AI Agent Stack** (the old-session import). **26 of 28 stacks are empty.** So the IMPORT NEVER RAN beyond those 2. The 67 `.enex` exports (~1.2 GB) live in **`C:\Users\dj\Documents\Evernote\Exported\`** (NOT Downloads), unprocessed. (SA reads fine; the calendar.py stdlib-shadow at C:\Users\dj means run list scripts from a clean dir / scrub sys.path.)

**THE IMPORT SCRIPT `process_enex_to_gdrive.py` — gaps before it can run:** (1) **stale `ENEX_DIR`** = `C:\Users\dj\Downloads\Evernote` → must point to `C:\Users\dj\Documents\Evernote\Exported`; (2) **no `credentials.json`** next to it and **no token.pickle** (⇒ never run) — needs a Desktop-OAuth credentials.json from the gen-lang-client-0790905441 "Odoo" project (or reuse the "Drive Folder Script"/"Drive Folder" desktop client); (3) it builds `Notebook/ <YYYY-MM-DD Title>/ → Google Doc + attachments` and `SKIP_EXISTING` checks only NOTEBOOK-folder existence — so the 26 empty skeleton folders would be SKIPPED, importing nothing. Fix: either delete the empty shells first, or change skip logic to check note-level emptiness. Big files (Tacoma 147 MB, Swamp Stuff 113 MB, Siding 103 MB) + Drive rate limits → expect a long run; make uploads resumable + add throttle/retry.

**IMPORT — COMPLETE 2026-06-28. 67/67 notebooks, 0 errors. 646 note-folders created (345 with text→Google Docs, 301 attachment-only→original files kept) + hundreds of attachments. Vault root now 66 folders (28 original stacks populated where shells existed = matched-shell; 17 new top-level folders created for notebooks with no shell; Accounting + AI Agent left as prior import). Spot-verified: Toyota Tacoma 2022=27 notes, BMW=15. Took 3 runs (2 crashes fixed mid-way — see below).**

**IMPORT — RAN 2026-06-28 (DJ greenlit).** Used `scratchpad/importer.py` (refined from process_enex_to_gdrive.py): reads the 67 .enex from `Documents\Evernote\Exported`, auths with the LIVE Render user-OAuth token (pulled from Render env via REST — NOT desktop flow), and for each notebook **resolves the target by name**: matches an EMPTY existing stack shell (e.g. Auto Stack/Toyota Prius 2011) and imports INTO it; if a same-name folder already has content → SKIP (idempotent, protects the 2 old ones); if no shell exists → CREATE a new folder at Vault root (DJ's choice — no "Unsorted"). Per note: subfolder `YYYY-MM-DD Title`, a Google Doc ONLY if the note has real text (≥3 chars after stripping tags+media → **skip empty stub Docs**), + attachments uploaded as-is (resumable chunked). ★ GOTCHA: lxml chokes on big notebooks ("huge text node, line… column…", >10 MB base64 attachment) → MUST use `etree.XMLParser(huge_tree=True, recover=True)`. Idempotent at notebook level (re-run skips populated, resumes empty), so safe to re-run after a crash. Run completed clean per-notebook log. Format VERIFIED on Toyota Prius test (text note→Doc, scan notes→just PDF, no stubs).

**FONT-END / VAULT UI — PHASE 1 SHIPPED 2026-06-28 (in-app, DJ wants to grow it to fully replace Evernote — Drive alone is "a good search tool but won't let me drop Evernote").** New owner page **`/owner/vault`** (tile 🗄️ "Vault" in index.html My Day group, next to Reference). `routers/owner/vault.py` (reuses `notes._drive_service` + `VAULT_ROOT_ID`; no-store HTML) + `static/owner/vault.html` (dark, mobile, breadcrumb browser, type-icon rows, debounced search). Endpoints: `GET /owner/api/vault/list?folder=<id>` (children folders+files + breadcrumb up to root) and `GET /owner/api/vault/search?q=` (Drive `fullText contains` — searches INSIDE PDFs/Docs via OCR — then `_ancestor_in_vault` filters to vault-only, memoized parent-walk, cap 50). Read-only. VERIFIED live: browse Auto Stack drill-down + search "Farmers" hit the scanned insurance PDFs. Registered in main.py (owner_vault). **PHASE 2 (grow, not built):** create/edit notes in any folder, upload anywhere, move/rename/delete, new notebooks. **PHASE 3:** inline PDF/image preview, tags across vault, customer links, shortcuts/favorites. Durability for this always-on app still hinges on the unsolved OAuth-expiry problem above. See [[project_drive_pdf_ocr_fulltext_search]].
3. Move the EIN PDFs into the vault (currently filed in Reference card "Window & Solar Care, LLC - CA SOS Filing", Odoo att 2211/2212 — see [[project_wsc_llc_formation]] / [[project_reference_quickref]]).

**Why:** central, reliable, always-accessible document vault = a real pillar of the owner app / north-star CRM ([[project_north_star_comprehensive_crm]]); replaces Evernote; must be Drive-backed for the volume and durable so it never "logs out."
**How to apply:** when building, go Drive-primary into folder 1uVXJjg4YYfqcijh4Vbvf-snWGfUIyb9Q via a service account; do NOT store the bulk files in Odoo. Fix the auth before relying on it.
